   # Simple Definitions

React:     a separate library

React-Dom: a separate library to work on the browser.

React-Native: a separate library to work on mobile.

Redux: core library 

React-Redux: To use Redux in react we use react-redux. (for wiring)

Flux:

Data flow is very strong in redux-toolkit.

Redux-toolkit: a toolkit for efficient Redux development.

reducers are used to change in stores.
      useselector : used to select.
      usedispatch: used to dispatch.

we use react-router dom like this..
        npm install react-router-dom

in the same way redux-toolkit like this...

        npm install @reduxjs/toolkit

  #  Interview Question

   onClick={() => dispatch(removeTodo(todo.id))}  

    // callback function is used to give reference and that when a user clicks then it functions

    Features = Slice